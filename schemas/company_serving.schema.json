{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Company Serving Schema",
  "description": "JSON schema for company data served by the API. Defines structure for domain, company_name, phones, social URLs, and address fields used in Elasticsearch index and API responses.",
  "type": "object"
}
